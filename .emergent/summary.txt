<analysis>**original_problem_statement:**

The user wants to implement a series of features and improvements for the S.T.E.W.A.R.D. Workspace Dashboard application.

**PRODUCT REQUIREMENTS:**

1.  **Command Wheel Feature:**
    *   Implement a circular, context-sensitive menu (Command Wheel) that appears on .
    *   The menu's options should change based on the cursor's context (empty workspace, over a window, over a CANVAS block, etc.).
    *   Implement a specific list of actions for each context (e.g., New Note on empty space, Focus on a window).
    *   The UI should have a Tony Stark holographic style, with neon rings, subtle animations, and a glass effect. The color should be influenced by the active Workzone.
    *   The Command Wheel was later refined to be smaller, more compact, and feature a multi-layered energetic core with a subtle breathing animation.

2.  **Window Management Bug Fix:**
    *   Fix a bug where maximizing a window by dragging it to the top of the screen causes it to overlap the bottom toolbar. The correct behavior should match the result of clicking the Maximize icon.

3.  **UI Tweak - Reorder Icons:**
    *   In the title bar of each window, reorder the control icons to be: Pin, Minimize, Maximize, Close.

4.  **Music Module - Library Playback:**
    *   Enable direct playback of media items by clicking on them in the Library tab.
    *   The UI should indicate the currently playing item.
    *   Clicking the currently playing item should toggle play/pause.

5.  **Music Module - Media Downloader:**
    *   Add a feature to download media (audio/video) from a URL (e.g., YouTube).
    *   The UI should include an input for the URL, options for format (MP3/MP4) and quality, and show download progress.
    *   After downloading, the item should be added to the Music Library.
    *   A Download to PC option should be available for the downloaded file.
    *   **CRITICAL ISSUE:** This feature is currently broken due to YouTube returning an .

**User's preferred language**: Czech

**what currently exists?**
The application is a full-stack S.T.E.W.A.R.D. Workspace Dashboard. In this session, the following were implemented:
- A Command Wheel context menu with a Tony Stark inspired UI.
- A fix for the window maximization bug.
- A UI tweak to reorder window control icons.
- A feature to allow direct playback from the Music module's library.
- A media downloader feature in the Music module, which is currently **broken**.

The backend is FastAPI, the frontend is React, and it uses a MongoDB database.

**Last working item**:
- Last item agent was working: The agent was attempting to resolve an  received from YouTube when trying to download certain videos using . After numerous failed attempts (updating libraries, changing headers, spoofing clients, using a JS runtime), the agent discovered that while direct HTTPS download links are blocked, HLS (m3u8) stream formats are available for the problematic video. The next step was to modify the backend to request and process these m3u8 streams instead.
- Status: IN PROGRESS
- Agent Testing Done: N
- Which testing method agent to use? backend testing agent
- User Testing Done: Y (User confirmed the 403 error persists with the latest attempted fixes)

**All Pending/In progress Issue list**:
- Issue 1: YouTube Downloader fails with  (P0)

Issues Detail:
- Issue 1:
    - **Attempted fixes**:
        - Updated  library.
        - Added various  including different user-agents and headers.
        - Installed  as a JavaScript runtime for  to solve challenges.
        - Switched from the  Python library to calling the CLI via .
        - Tried multiple flags like , .
        - These all failed because YouTube is actively blocking direct HTTPS download requests from the server's IP for certain videos.
    - **Next debug checklist**:
        1.  Modify the  function in .
        2.  Change the  format selection options to prioritize HLS/m3u8 streams over direct HTTPS links for MP4/MP3 downloads. The CLI output from message 315 confirms these formats are available. A format string like  might work, as  can handle HLS streams.
        3.  Ensure  is used correctly for merging/transcoding if separate audio/video streams are downloaded via HLS.
    - **Why fix this issue and what will be achieved with the fix?**: This is the core blocker for the media downloader feature. Fixing it will make the feature functional as requested by the user.
    - **Status**: IN PROGRESS
    - **Is recurring issue?**: Y
    - **Should Test frontend/backend/both after fix?**: backend
    - **Blocked on other issue**: None

**In progress Task List**:
There are no in-progress tasks, only the P0 issue above.

**Upcoming and Future Tasks**
- **Task 1: Implement Download to PC Functionality (P1)**
    - The UI button Uložit do PC (Save to PC) exists, but there is no backend logic to serve the downloaded file to the user's browser.
    - An endpoint needs to be created in  that takes a filename and returns a .
    - The frontend  function in  needs to be implemented to trigger this download.
    - **Status:** NOT STARTED

- **Task 2: Finalize Format/Quality Selection (P2)**
    - The UI for selecting format and quality was created, but the backend logic was simplified to hardcode formats during debugging of the 403 error.
    - Once the 403 error is resolved, the backend  function needs to be updated to respect the  sent from the frontend.
    - **Status:** NOT STARTED
    - **Blocked on:** Issue 1: YouTube Downloader fails with 

**Completed work in this session**
- **Command Wheel Feature:** A fully functional and visually styled contextual menu was implemented.
  - 
  - 
- **Window Maximization Bug:** Fixed the issue where dragging a window to the top caused it to overlap the bottom toolbar.
  - 
- **Reordered Window Icons:** The control icons in the window title bar were reordered as requested.
  - 
- **Music Library Playback:** Enabled direct playback of tracks from the library list.
  - 
- **Media Downloader (Initial Version):** Created the backend endpoints and frontend UI for downloading media. This version worked initially before running into persistent 403 errors.
  - 
  - 

**Earlier issues found/mentioned but not fixed**
None.

**Known issue recurrence from previous fork**
None.

**Code Architecture**


**Key Technical Concepts**
-   **Frontend**: React, React Hooks (useState, useEffect, useContext), CSS-in-JS, SVG, Flexbox.
-   **Backend**: FastAPI, Pydantic.
-   **Integrations**:  for fetching information and downloading from YouTube.
-   **System**:  for media post-processing,  as a JS runtime for .

**key DB schema**
No database schema changes were made in this session.

**changes in tech stack**
-   **Backend**: Added , , and  to support the media downloading feature.

**All files of reference**
-   : Modified to add  and  endpoints and the logic for .
-   : Modified to add .
-   : Heavily modified to add library playback and the entire UI and logic for the media downloader.
-   : Created and modified multiple times to implement the Command Wheel and its visual refinements.
-   : Created to handle the state and logic for the Command Wheel.
-   : Modified to add new actions for the Command Wheel and to fix the window maximization bug.
-   : Modified to reorder icons and add data-attributes for the Command Wheel.

**Areas that need refactoring**
-   The  and  functions in  are cluttered with commented-out code and remnants of multiple debugging attempts. Once the 403 error is definitively solved, these functions should be cleaned up to contain only the working logic.

**key api endpoints**
-   : Body: . Fetches metadata for a given media URL.
-   : Body: . Streams the download progress and saves the file on the server.

**Critical Info for New Agent**
-   The highest priority is fixing the  error in the YouTube downloader.
-   Do not waste time on previous failed approaches (simple header changes, different clients). The evidence points to YouTube blocking direct HTTPS download links for certain videos.
-   The most promising next step is to modify 's options in  to use HLS/m3u8 streams, which were confirmed to be available for the problematic video.
-   After fixing the download, remember to implement the Download to PC functionality, as it's a required part of the feature that hasn't been built yet.

**documents and test reports created in this job**
None.

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** Reports the download fails with .
2.  **Agent:** Tries updating  and adding better configuration.
3.  **User:** Confirms the 403 error persists.
4.  **Agent:** Tries installing  as a JS runtime to bypass YouTube's protection.
5.  **User:** Confirms the 403 error persists.
6.  **Agent:** Switches from the Python library to a direct CLI call via .
7.  **User:** Confirms the 403 error persists, providing a detailed log showing the failure point.
8.  **Agent:** Investigates and finds the specific video is more protected than others.
9.  **Agent:** Tries to use different download formats (HLS/m3u8) as the next attempt.
10. **(No pending user message)** The agent was in the middle of diagnosing the issue when the session ended.

**Project Health Check:**
-   **Broken**: The media downloader feature in the Music module is non-functional due to the  error.

**3rd Party Integrations**
-   : Used for downloading media from YouTube and other sites.

**Testing status**
-   Testing agent used after significant changes: NO
-   Troubleshoot agent used after agent stuck in loop: NO
-   Test files created: []
-   Known regressions: The media downloader was working at one point for a specific video but now fails for others, which can be considered a regression or a discovery of a more complex edge case.

**Credentials to test flow:**
N/A

**What agent forgot to execute**
-   The agent created the Download to PC button in the frontend UI but never implemented the corresponding backend endpoint or frontend logic to actually serve the downloaded file to the user's browser. This part of the original request is incomplete.</analysis>
