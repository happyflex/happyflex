<analysis>**original_problem_statement:**
The user wants to improve the S.T.E.W.A.R.D. Workspace Dashboard by fixing several UI bugs and implementing a comprehensive layout management system.

**Initial Bug Fixes (Completed):**
1.  **Snap Mode Gutter:** Introduce a 30px gap between windows snapped to the left and right sides.
2.  **Drag Behavior Fix:** Eliminate the jump that occurs when starting to drag a window or an element within the Projects module.
3.  **Top Resize Fix:** Stabilize the resize behavior when dragging the top edge of a window to prevent it from jumping or calculating incorrect dimensions, especially with fast mouse movements.

**Main Feature Request (In Progress):**
*   **Robust Layout Saving & Loading:** Implement a system to save and load the entire workspace state. When a layout is loaded, it must restore not just the windows, but a complete snapshot of work, including:
    *   Window positions, sizes, and stacking order ().
    *   Window states like , , and .
    *   All items present in the CANVAS area (the right-side deferred module list).
    *   The internal view state of each module instance. For example:
        *   **Music Module:** The active tab (Player/Library), whether mini-mode is on, and the selected playlist.
        *   **Goals Module:** The specific goal being viewed and the active sub-section (e.g., Planning).
        *   **Files Module:** The currently open folder.
        *   **People Module:** The specific contact detail being viewed.
    *   A  timestamp for each saved layout, which updates every time the layout is loaded.

**User's preferred language**: Czech. The next agent MUST respond in Czech.

**what currently exists?**
The project is a React-based workspace dashboard (S.T.E.W.A.R.D.) with a FastAPI backend. The initial UI bug fixes (drag jump, top resize) have been successfully implemented and tested. A layout saving/loading system has been built, which correctly restores window positions, sizes, pin/focus states, and CANVAS items.

However, the final part of the implementation—restoring the internal view state of modules—has introduced a critical bug. The application now crashes on startup with a Maximum update depth exceeded error, indicating an infinite re-render loop.

**Last working item**:
*   **Last item agent was working:** The agent was trying to fix a Maximum update depth exceeded crash. This was caused by implementing a new props-based system (, ) for synchronizing the module's internal view state with the central . The implementation calls the  callback from within a  hook in each module without a proper conditional guard, triggering an infinite loop of re-renders.
*   **Status:** BLOCKED
*   **Agent Testing Done:** N (The application is not runnable).
*   **Which testing method agent to use?** First, the agent must fix the crash. After the fix, use the  to manually test the complete layout saving/loading workflow as described by the user.
*   **User Testing Done:** N

**All Pending/In progress Issue list**:
*   **Issue 1:** The application crashes on startup with a Maximum update depth exceeded error. (Priority: P0 - Blocker)
*   **Issue 2:** The View State of modules is not restored when loading a layout. (Priority: P1)

**Issues Detail:**
*   **Issue 1: Application Crash (Maximum update depth exceeded)**
    *   **Attempted fixes:** The agent refactored the view state logic from a  event-driven approach to a props-based system. This was the correct architectural change but was implemented incorrectly, causing the crash. The agent identified the cause is an unguarded callback in a  hook.
    *   **Next debug checklist:**
        1.  Locate the  hook responsible for calling  in each of the following module files: , , , , , , and .
        2.  Wrap the  call in a condition to prevent it from running on every render. The call should only execute if the new view state is different from the previously emitted view state.
        3.  A standard pattern for this is to use a  to store the last emitted state and perform a deep comparison before calling the function. Example: 
    *   **Why fix this issue and what will be achieved with the fix?** This is a critical blocker. The application is completely unusable until this crash is resolved.
    *   **Status:** IN PROGRESS
    *   **Is recurring issue?** N
    *   **Should Test frontend/backend/both after fix?** Frontend
    *   **Blocked on other issue:** None. This is the primary blocker.

*   **Issue 2: Module View State Not Restoring on Layout Load**
    *   **Attempted fixes:** The correct architecture (passing  as a prop and  as a callback) is in place but is non-functional due to the crash (Issue 1).
    *   **Next debug checklist:**
        1.  First, resolve Issue 1 to make the application runnable.
        2.  Once the app is stable, perform a full validation test:
            a. Open the Music module, select a playlist, and enable mini-mode.
            b. Open the People module and view a contact's details.
            c. Save the current workspace as a new layout.
            d. Reset the workspace (e.g., close modules or change their states).
            e. Load the saved layout.
            f. Verify that the Music module is restored to mini-mode with the correct playlist and the People module is showing the correct contact detail.
    *   **Why fix this issue and what will be achieved with the fix?** This will complete the user's core feature request for a fully persistent workspace.
    *   **Status:** BLOCKED
    *   **Is recurring issue?** Y
    *   **Should Test frontend/backend/both after fix?** Frontend
    *   **Blocked on other issue:** Issue 1: Application Crash (Maximum update depth exceeded)

**In progress Task List**:
*   **Task 1: Stabilize and Complete the Layout View State Restoration Feature**
    *   **Where to resume:** Begin by editing the module components (e.g., ) to add the conditional guard around the  call to fix the infinite loop.
    *   **What will be achieved with this?** A stable, fully-functional layout management system that restores the complete workspace, including the internal state of each module.
    *   **Status:** BLOCKED
    *   **Should Test frontend/backend/both after fix?** Frontend
    *   **Blocked on something:** The application is crashing.

**Upcoming and Future Tasks**
*   There are no other pending tasks. The entire focus is on fixing the current crash and completing the layout saving feature.

**Completed work in this session**
- **Drag Jump Fix:** Implemented a shared coordinate conversion helper () and refactored  and  to eliminate the drag offset.
- **Top Resize Fix:** Reworked the resize logic in  using a stable delta-based calculation, fixing erratic behavior during top-edge resizing.
- **Layout System Foundation:**
    - Expanded the data saved for each layout in  to include , , and CANVAS items.
    - Re-architected module view state handling from an unreliable  event system to a robust props-based system (, ).

**Earlier issues found/mentioned but not fixed**
*   None. All previous issues were either resolved or led to the current blocking bug.

**Known issue recurrence from previous fork**
*   The previous fork was blocked by failing automated UI tests (). The current agent worked around this by using manual testing and code-review agents. This underlying fragility in automated testing might reappear if complex tests are required in the future.

**Code Architecture**


**Key Technical Concepts**
*   **React State Management:** Centralized state in  with heavy use of , , .
*   **Component Communication:** A props-based pattern ( prop down,  callback up) is used to synchronize state between the central context and child modules.
*   **Infinite Re-render Loop:** The primary bug is a classic React issue where a  triggers a state update in a parent, which causes a re-render of the child, which re-triggers the , leading to a crash.

**key DB schema**
N/A. Persistence is handled via the browser's .

**All files of reference**
*   **Files to Fix (Source of Crash):**
    *   
    *   
    *   
    *   
    *   
    *   
    *   
*   **Core Logic Files:**
    *    (Defines and manages the state).
    *    (Passes the state props to modules).
    *    (Orchestrates save/load).

**Critical Info for New Agent**
*   **TOP PRIORITY: FIX THE CRASH.** The application is unusable. The crash is a Maximum update depth exceeded error caused by an infinite re-render loop.
*   **THE CAUSE:**  hooks in the module components are calling the  callback on every render.
*   **THE FIX:** You must edit each module component and add a condition to its  hook. The  callback should only be called if the module's internal view state has actually changed since the last time the callback was invoked. Use a  to cache the last-sent state and compare against it.
*   Once the crash is fixed, you must perform the full user-defined test scenario to verify that the internal view state of modules is correctly saved and restored.

**documents and test reports created in this job**
*   
*   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User (message 302):** Provided a final, detailed problem statement explaining that module View State is not being restored correctly upon loading a layout. Listed specific examples for several modules (Goals, Files, Music, etc.). This is the primary unresolved task.
2.  **User (message 325):** pokračujeme bejbyy! (Let's continue, baby!) - Encouragement to keep working.
3.  **User (message 267):** Confirmed that layout loading works for window state, CANVAS, and focus mode, but reiterated that module View State (details, tabs, mini-mode) is still not being restored.
4.  **User (message 240):** Confirmed that window state and CANVAS are loading correctly, but focus mode and view state are not.
5.  **User (message 209):** Reported that the layout saving feature was not working as intended.
6.  **User (message 150):** Provided a detailed problem statement for the comprehensive layout saving feature, including restoring window state, module view state, and CANVAS items.
7.  **User (message 123):** Provided a detailed problem statement for fixing the buggy top resize behavior on windows. (This is completed).
8.  **User (message 84):** Teď ten drag vůbec nejde. (Now the drag doesn't work at all.) - Reported that a previous fix had broken dragging entirely. (This was subsequently fixed).
9.  **User (message 4):** Provided a very detailed, technical problem statement for fixing the jump on drag issue by unifying the coordinate system. (This is completed).

**Project Health Check:**
*   **Broken:** The entire frontend application is non-functional and crashes on startup.

**3rd Party Integrations**
*   None.

**Testing status**
*   **Testing agent used after significant changes:** YES (but before the final breaking change).
*   **Troubleshoot agent used after agent stuck in loop:** NO
*   **Test files created:** None.
*   **Known regressions:** The entire application is a regression from a previously working state due to the crash.

**What agent forgot to execute**
*   The agent failed to add a defensive condition when implementing the  callback, introducing a classic infinite re-render loop that it did not catch before finishing its work. This left the application in a completely broken state.</analysis>
